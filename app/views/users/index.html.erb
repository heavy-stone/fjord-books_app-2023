<h1><%= t('views.common.title_index', name: i18n_pluralize(User.model_name.human)) %></h1>

<table>
  <thead>
    <tr>
      <th><%= User.human_attribute_name(:email) %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to t('views.common.show', name: User.model_name.human), user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @users %>
